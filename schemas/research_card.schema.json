{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "skex://schemas/research_card.v1",
  "title": "SKEX Research Card v1",
  "description": "Every key is required. Use null when the document does not support that field. A non-null field must have a quote in evidence_spans that is a contiguous substring of the document. A score that is present still needs metric and value. Do not add keys.",
  "type": "object",
  "additionalProperties": false,
  "required": ["task", "method", "datasets", "metrics", "scores", "claims", "limitations", "evidence_spans"],
  "properties": {
    "task": { "type": ["array", "null"], "items": { "type": "string" } },
    "method": { "type": ["array", "null"], "items": { "type": "string" } },
    "datasets": { "type": ["array", "null"], "items": { "type": "string" } },
    "metrics": { "type": ["array", "null"], "items": { "type": "string" } },
    "scores": {
      "type": ["array", "null"],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["metric", "value"],
        "properties": {
          "metric": { "type": "string" },
          "value": { "type": ["number", "string", "null"] },
          "condition": { "type": ["string", "null"] }
        }
      }
    },
    "claims": { "type": ["array", "null"], "items": { "type": "string" } },
    "limitations": { "type": ["array", "null"], "items": { "type": "string" } },
    "evidence_spans": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["field", "quote"],
        "properties": {
          "field": { "type": "string" },
          "quote": { "type": "string" }
        }
      }
    }
  }
}
